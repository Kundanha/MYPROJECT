import { Login } from './login';
import React from 'react';

interface Props {
  // Define props here
}

const Page: React.FC<Props> = (props) => {
  return (
    <Login/>
  );
};

export default Page;